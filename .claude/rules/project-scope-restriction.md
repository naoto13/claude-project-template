---
description: プロジェクトディレクトリ外へのアクセスを制限する
alwaysApply: true
---

# プロジェクトスコープ制限

## 絶対ルール
ファイル操作は原則として現在のプロジェクトルート配下に限定する。

## 許可される操作
- 現在のプロジェクトルート配下の操作
- 一時ファイル置き場
- ツールが明示的に許可するメモリや設定ディレクトリ

## 禁止される操作
- 他プロジェクトへの不用意なアクセス
- `~/.ssh/`, `~/.aws/`, `~/.env` など秘密情報ディレクトリへのアクセス
- 理由のないホームディレクトリ横断探索
