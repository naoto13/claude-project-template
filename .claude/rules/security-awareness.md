# Security Awareness Rules

## 必須ルール

### 1. 秘密鍵・APIキーのハードコード禁止
- 秘密鍵、API キー、接続文字列、シードフレーズをコードに直接書かない
- 必ず `.env` または環境変数から読み込む
- ログに秘密情報を出力しない

### 2. 入力値の検証
- 外部入力は信頼しない
- 外部 API のレスポンスも型・構造を検証してから使用する
- 認可漏れや IDOR をレビュー対象に含める
