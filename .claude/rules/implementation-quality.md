# Implementation Quality Rules

## 必須ルール

### 1. 空実装禁止
`pass`、`...`、`TODO` だけの実装は不可。

### 2. 秘密情報はコードに埋め込まない
- `.env` または環境変数を使う
- ログに出さない

### 3. 実装後は実行して確認する
完了報告前に、少なくとも変更箇所に対応する実行確認を行う。

## 推奨ルール
- 最小限の実装から始める
- 既存パターンを踏襲する
- 例外を握り潰さない
- 横断変更は事前に Grep / Glob で影響範囲を把握する
