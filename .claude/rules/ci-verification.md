# CI 検証ルール

## 必須ルール

### 作業終了時に検証を実行する
作業完了前に、そのプロジェクトで定義されている検証コマンドを必ず実行する。

標準例:

```bash
pnpm test
pnpm check
pnpm build
```

### 確認項目
1. 修正点に対応するテストが存在し、パスしている
2. 既存テストが壊れていない
3. 型チェックや静的検証がパスしている
4. ビルドが通る

### 禁止事項
- 検証未実行のまま完了報告しない
- 失敗を無視して完了扱いにしない
- 使えないコマンドがある場合に説明なしで省略しない
